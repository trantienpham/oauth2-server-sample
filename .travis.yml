language: node_js
node_js:
  - "stable"
env:
  - NODE_ENV=production
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.16.0
  - export PATH="$HOME/.yarn/bin:$PATH"
install:
  - yarn install
cache:
  directories:
    - "node_modules"
script:
  - yarn test